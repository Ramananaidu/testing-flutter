name: Flutter Build APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      
      - name: Build APK
        run: |          
          chmod +x AtdMobileApp/gradlew
          cd AtdMobileApp && ./gradlew assembleRelease
          pwd && ls -la
          cd /home/runner/work/testing-flutter/testing-flutter/AtdMobileApp/build && ls -la
